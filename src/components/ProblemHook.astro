---
import Section from './Section.astro';
---

<Section id="features" background="muted">
  <div class="max-w-5xl mx-auto">
    <!-- Problem -->
    <div class="max-w-3xl mx-auto text-center mb-16">
      <!-- Icon -->
      <div class="flex justify-center mb-8 reveal">
        <div class="w-14 h-14 rounded-2xl bg-secondary/10 flex items-center justify-center">
          <svg class="w-7 h-7 text-secondary" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
            <path stroke-linecap="round" stroke-linejoin="round" d="M12 9v3.75m-9.303 3.376c-.866 1.5.217 3.374 1.948 3.374h14.71c1.73 0 2.813-1.874 1.948-3.374L13.949 3.378c-.866-1.5-3.032-1.5-3.898 0L2.697 16.126zM12 15.75h.007v.008H12v-.008z" />
          </svg>
        </div>
      </div>

      <h2 class="font-heading font-bold text-3xl md:text-4xl mb-6 reveal">
        Instantly checking your website updates across screen sizes shouldn't be this hard
      </h2>
      <p class="text-foreground/60 text-lg leading-relaxed mb-6 reveal reveal-delay-1">
        You push a change and want to see how it looks. But your preview is buried in a dashboard somewhere — Cloudflare, GitHub, Vercel, Netlify — and even when you find the link, you can only see it at whatever screen size you're on.
      </p>
      <p class="text-foreground/60 text-lg leading-relaxed mb-8 reveal reveal-delay-2">
        Want to check the desktop layout from your phone? You're stuck waiting until you're back at your desk.
      </p>

      <!-- Transition line -->
      <div class="reveal reveal-delay-3 flex items-center justify-center gap-4">
        <div class="h-px w-12 bg-primary/30"></div>
        <p class="text-primary dark:text-indigo-300 font-heading font-semibold text-lg">
          There's a better way
        </p>
        <div class="h-px w-12 bg-primary/30"></div>
      </div>
    </div>

    <!-- Solution -->
    <div class="grid lg:grid-cols-2 gap-12 lg:gap-16 items-center">
      <!-- Text -->
      <div class="reveal">
        <h3 class="font-heading font-bold text-2xl md:text-3xl mb-6">
          View any resolution, from any device — instantly
        </h3>

        <p class="text-foreground/60 text-lg leading-relaxed mb-8">
          Check the desktop layout on your iPhone during your morning walk. Switch to the iPad view before your standup. Viewpo's viewport viewer renders the actual site — not a screenshot — at any resolution you choose.
        </p>

        <ul class="space-y-4">
          <li class="flex items-start gap-3">
            <div class="w-6 h-6 rounded-full bg-success/10 flex items-center justify-center flex-shrink-0 mt-0.5">
              <svg class="w-3.5 h-3.5 text-success" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5">
                <path stroke-linecap="round" stroke-linejoin="round" d="M4.5 12.75l6 6 9-13.5" />
              </svg>
            </div>
            <div>
              <span class="font-heading font-semibold text-sm">Any resolution</span>
              <p class="text-foreground/50 text-sm">iPhone, iPad, laptop, 27-inch desktop, custom sizes — all from whatever device you're holding</p>
            </div>
          </li>
          <li class="flex items-start gap-3">
            <div class="w-6 h-6 rounded-full bg-success/10 flex items-center justify-center flex-shrink-0 mt-0.5">
              <svg class="w-3.5 h-3.5 text-success" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5">
                <path stroke-linecap="round" stroke-linejoin="round" d="M4.5 12.75l6 6 9-13.5" />
              </svg>
            </div>
            <div>
              <span class="font-heading font-semibold text-sm">Real browser engine</span>
              <p class="text-foreground/50 text-sm">Loads the actual website in a real browser engine — works with every site, even those that block iframes</p>
            </div>
          </li>
          <li class="flex items-start gap-3">
            <div class="w-6 h-6 rounded-full bg-success/10 flex items-center justify-center flex-shrink-0 mt-0.5">
              <svg class="w-3.5 h-3.5 text-success" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5">
                <path stroke-linecap="round" stroke-linejoin="round" d="M4.5 12.75l6 6 9-13.5" />
              </svg>
            </div>
            <div>
              <span class="font-heading font-semibold text-sm">Pan, scroll, and interact</span>
              <p class="text-foreground/50 text-sm">Navigate around a desktop-sized layout on your phone, like looking through a viewfinder</p>
            </div>
          </li>
        </ul>
      </div>

      <!-- Visual: Device comparison -->
      <div class="reveal reveal-delay-2">
        <div class="relative">
          <!-- Desktop frame (background, larger) -->
          <div class="w-full aspect-[16/10] rounded-xl border-2 border-foreground/10 dark:border-white/10 bg-white dark:bg-slate-800/30 p-2">
            <!-- Desktop toolbar -->
            <div class="h-6 bg-foreground/[0.03] dark:bg-white/[0.03] rounded-t-lg border-b border-foreground/5 dark:border-white/5 flex items-center px-3 gap-1.5">
              <div class="w-2 h-2 rounded-full bg-red-400/50"></div>
              <div class="w-2 h-2 rounded-full bg-yellow-400/50"></div>
              <div class="w-2 h-2 rounded-full bg-green-400/50"></div>
              <div class="flex-1 flex justify-center">
                <div class="text-[8px] text-foreground/20 dark:text-white/20 font-mono">your-project.pages.dev</div>
              </div>
            </div>
            <!-- Desktop content -->
            <div class="h-full bg-white dark:bg-slate-900 rounded-b-lg p-4">
              <div class="w-24 h-3 bg-slate-200 dark:bg-slate-700 rounded mb-4"></div>
              <div class="w-full h-2.5 bg-slate-100 dark:bg-slate-800 rounded mb-2"></div>
              <div class="w-3/4 h-2.5 bg-slate-100 dark:bg-slate-800 rounded mb-4"></div>
              <div class="grid grid-cols-3 gap-2">
                <div class="h-16 bg-slate-50 dark:bg-slate-800/50 rounded-lg border border-slate-100 dark:border-slate-700"></div>
                <div class="h-16 bg-slate-50 dark:bg-slate-800/50 rounded-lg border border-slate-100 dark:border-slate-700"></div>
                <div class="h-16 bg-slate-50 dark:bg-slate-800/50 rounded-lg border border-slate-100 dark:border-slate-700"></div>
              </div>
            </div>
          </div>

          <!-- Phone frame (foreground, overlapping) -->
          <div class="absolute -bottom-6 -right-4 md:-right-6 w-28 sm:w-32">
            <div class="rounded-2xl border-2 border-primary/30 bg-white dark:bg-slate-900 p-1.5 shadow-lg shadow-primary/10">
              <!-- Phone notch -->
              <div class="w-10 h-2 bg-foreground/10 dark:bg-white/10 rounded-full mx-auto mb-1"></div>
              <!-- Same content, scaled in phone -->
              <div class="rounded-xl bg-slate-50 dark:bg-slate-800 p-2 aspect-[9/16]">
                <div class="w-10 h-1.5 bg-slate-200 dark:bg-slate-700 rounded mb-2"></div>
                <div class="w-full h-1 bg-slate-100 dark:bg-slate-700/50 rounded mb-1"></div>
                <div class="w-3/4 h-1 bg-slate-100 dark:bg-slate-700/50 rounded mb-2"></div>
                <div class="grid grid-cols-3 gap-0.5">
                  <div class="h-4 bg-white dark:bg-slate-700 rounded border border-slate-100 dark:border-slate-600"></div>
                  <div class="h-4 bg-white dark:bg-slate-700 rounded border border-slate-100 dark:border-slate-600"></div>
                  <div class="h-4 bg-white dark:bg-slate-700 rounded border border-slate-100 dark:border-slate-600"></div>
                </div>
              </div>
            </div>
            <!-- Label -->
            <div class="text-center mt-2">
              <span class="text-[10px] font-heading font-semibold text-primary dark:text-indigo-300">Same site, any size</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</Section>

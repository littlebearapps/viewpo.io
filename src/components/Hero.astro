---
import Button from './Button.astro';
---

<section class="relative overflow-hidden py-20 md:py-32">
  <div class="container-wide">
    <div class="max-w-3xl mx-auto text-center">
      <!-- Badge -->
      <div class="hero-entrance inline-flex items-center gap-2 bg-primary/10 text-primary dark:text-indigo-300 text-sm font-medium px-4 py-1.5 rounded-full mb-8">
        <span class="w-2 h-2 bg-success rounded-full animate-pulse"></span>
        Coming soon to iOS &amp; macOS
      </div>

      <!-- Headline -->
      <h1 class="hero-entrance hero-entrance-delay-1 font-heading font-bold text-4xl md:text-5xl lg:text-6xl leading-tight tracking-tight mb-6">
        See your preview deploys.
        <span class="gradient-text-primary">On any device.</span>
        From your phone.
      </h1>

      <!-- Subheading -->
      <p class="hero-entrance hero-entrance-delay-2 text-lg md:text-xl text-foreground/60 max-w-2xl mx-auto mb-10 leading-relaxed">
        Viewpo aggregates preview deployments from Cloudflare Pages, Vercel, Netlify, and GitHub Pages into one dashboard — with a viewport viewer that shows any screen size, right on your phone.
      </p>

      <!-- CTAs -->
      <div class="hero-entrance hero-entrance-delay-3 flex flex-col sm:flex-row items-center justify-center gap-4">
        <Button href="#cta" size="lg" icon="arrow">
          Join TestFlight Beta
        </Button>
        <Button href="#features" variant="outline" size="lg">
          See How It Works
        </Button>
      </div>
    </div>

    <!-- Viewport Demo Placeholder -->
    <div class="hero-entrance hero-entrance-delay-4 mt-16 md:mt-24 max-w-4xl mx-auto">
      <div class="relative rounded-2xl border-2 border-foreground/10 dark:border-white/10 bg-muted dark:bg-slate-800/50 overflow-hidden shadow-lg aspect-[16/9] flex items-center justify-center">
        <!-- Simulated viewport viewer UI -->
        <div class="absolute top-0 left-0 right-0 h-10 bg-foreground/5 dark:bg-white/5 border-b border-foreground/10 dark:border-white/10 flex items-center px-4 gap-2">
          <div class="flex gap-1.5">
            <div class="w-3 h-3 rounded-full bg-red-400/60"></div>
            <div class="w-3 h-3 rounded-full bg-yellow-400/60"></div>
            <div class="w-3 h-3 rounded-full bg-green-400/60"></div>
          </div>
          <div class="flex-1 flex items-center justify-center">
            <div class="text-xs text-foreground/40 font-medium bg-foreground/5 dark:bg-white/5 px-3 py-0.5 rounded">
              Desktop 2560 &times; 1440
            </div>
          </div>
          <div class="flex gap-2">
            <div class="w-6 h-4 rounded border border-foreground/20 dark:border-white/20"></div>
            <div class="w-4 h-4 rounded border border-foreground/20 dark:border-white/20"></div>
            <div class="w-3 h-5 rounded border border-primary/60"></div>
          </div>
        </div>
        <!-- Content placeholder -->
        <div class="pt-10 p-8 w-full h-full flex flex-col items-center justify-center gap-4">
          <div class="w-16 h-16 rounded-xl bg-primary/20 flex items-center justify-center">
            <svg class="w-8 h-8 text-primary" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
              <path stroke-linecap="round" stroke-linejoin="round" d="M9 17.25v1.007a3 3 0 01-.879 2.122L7.5 21h9l-.621-.621A3 3 0 0115 18.257V17.25m6-12V15a2.25 2.25 0 01-2.25 2.25H5.25A2.25 2.25 0 013 15V5.25A2.25 2.25 0 015.25 3h13.5A2.25 2.25 0 0121 5.25z" />
            </svg>
          </div>
          <p class="text-sm text-foreground/40 font-medium">Product screenshots coming soon</p>
        </div>
      </div>
    </div>
  </div>
</section>

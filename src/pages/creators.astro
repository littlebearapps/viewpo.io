---
import BaseLayout from '@layouts/BaseLayout.astro';
import Header from '@components/Header.astro';
import Footer from '@components/Footer.astro';
import Section from '@components/Section.astro';
import FeatureCard from '@components/FeatureCard.astro';
import Button from '@components/Button.astro';

const providers = [
  { name: 'Cloudflare Pages', note: 'Used by Cursor, Bolt' },
  { name: 'Vercel', note: 'Used by v0, Lovable' },
  { name: 'Netlify', note: 'Used by Bolt, Lovable, Replit' },
  { name: 'GitHub Pages', note: 'Used by Cursor, Replit' },
];

const creatorFaqItems = [
  {
    question: 'Do I need coding knowledge to use Viewpo?',
    answer: 'No. If you can deploy a website through Cloudflare Pages, Vercel, Netlify, or GitHub Pages — whether you did it yourself or an AI tool did it for you — Viewpo works. You link your account, and Viewpo handles the rest.',
  },
  {
    question: 'I built my site with Bolt. Will Viewpo work?',
    answer: 'Yes. Bolt typically deploys to Netlify. Once you connect your Netlify account to Viewpo, your Bolt-deployed sites appear automatically. The same goes for Lovable, v0, Cursor, and any tool that deploys to a supported provider.',
  },
  {
    question: 'What if I used Cursor or v0?',
    answer: 'Cursor and v0 projects often deploy to Vercel or Cloudflare Pages. Connect the relevant account in Viewpo and your projects show up. Viewpo doesn\'t care how the code was written — it just needs to know where it\'s deployed.',
  },
  {
    question: 'Is Viewpo free?',
    answer: 'Yes. The free tier includes up to 3 projects, 1 provider connection, and full access to the viewport viewer at any screen size. No credit card required. Upgrade to Starter ($9/month) when you need unlimited projects and all providers.',
  },
  {
    question: 'Can I share a preview with someone to get their opinion?',
    answer: 'Yes — on the Starter plan and above, you can generate a shareable link or QR code for any preview. Send it to a friend, a client, or post it in a group chat. They can see your site at any screen size without needing an account.',
  },
  {
    question: 'How is this different from just resizing my browser?',
    answer: 'Resizing your browser shows you a rough approximation, but it doesn\'t match how the site actually renders on a phone or tablet. Viewpo uses a real browser engine to load the site at the exact resolution of the device you\'re simulating — so you see what real users see.',
  },
];
---

<BaseLayout
  title="Viewpo for Creators — Preview on Every Screen | Little Bear Apps"
  description="Built something with AI? Check how it looks on a phone, tablet, and desktop — before anyone else sees it. Works with Cursor, v0, Bolt, Lovable, and Replit projects."
>
  <Header slot="header" />

  <!-- Hero -->
  <section class="relative overflow-hidden py-20 md:py-32">
    <!-- Ambient glow orbs -->
    <div class="pointer-events-none absolute -top-40 -left-40 w-[500px] h-[500px] rounded-full bg-primary/20 blur-[120px] dark:bg-primary/10" aria-hidden="true"></div>
    <div class="pointer-events-none absolute -bottom-20 -right-32 w-[400px] h-[400px] rounded-full bg-secondary/15 blur-[100px] dark:bg-secondary/[0.08]" aria-hidden="true"></div>

    <div class="container-wide">
      <div class="max-w-3xl mx-auto text-center">
        <div class="hero-entrance inline-flex items-center gap-2 bg-secondary/10 text-secondary dark:text-orange-300 text-sm font-medium px-4 py-1.5 rounded-full mb-8">
          <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M9.813 15.904L9 18.75l-.813-2.846a4.5 4.5 0 00-3.09-3.09L2.25 12l2.846-.813a4.5 4.5 0 003.09-3.09L9 5.25l.813 2.846a4.5 4.5 0 003.09 3.09L15.75 12l-2.846.813a4.5 4.5 0 00-3.09 3.09zM18.259 8.715L18 9.75l-.259-1.035a3.375 3.375 0 00-2.455-2.456L14.25 6l1.036-.259a3.375 3.375 0 002.455-2.456L18 2.25l.259 1.035a3.375 3.375 0 002.455 2.456L21.75 6l-1.036.259a3.375 3.375 0 00-2.455 2.456zM16.894 20.567L16.5 21.75l-.394-1.183a2.25 2.25 0 00-1.423-1.423L13.5 18.75l1.183-.394a2.25 2.25 0 001.423-1.423l.394-1.183.394 1.183a2.25 2.25 0 001.423 1.423l1.183.394-1.183.394a2.25 2.25 0 00-1.423 1.423z" />
          </svg>
          For AI Builders &amp; Creators
        </div>

        <h1 class="hero-entrance hero-entrance-delay-1 font-heading font-bold text-4xl md:text-5xl lg:text-6xl leading-tight tracking-tight mb-6">
          See your website on
          <span class="gradient-text-primary">every screen size.</span>
        </h1>

        <p class="hero-entrance hero-entrance-delay-2 text-lg md:text-xl text-foreground/60 max-w-2xl mx-auto mb-10 leading-relaxed">
          Built something with AI? Check how it looks on a phone, tablet, and desktop — before anyone else sees it. Viewpo shows you the real site at any screen size, right from your phone.
        </p>

        <div class="hero-entrance hero-entrance-delay-3 flex flex-col sm:flex-row items-center justify-center gap-4">
          <Button href="/#cta" size="lg" icon="arrow" class="plausible-event-name=Creators+CTA+Join+Beta">
            Check Your Site — Free
          </Button>
          <Button href="/#pricing" variant="outline" size="lg">
            See Pricing
          </Button>
        </div>
      </div>
    </div>
  </section>

  <!-- Problem -->
  <Section background="muted">
    <div class="max-w-3xl mx-auto text-center">
      <h2 class="font-heading font-bold text-3xl md:text-4xl mb-6 reveal">
        You built it. But does it look right?
      </h2>
      <p class="text-foreground/60 text-lg leading-relaxed mb-6 reveal reveal-delay-1">
        You used AI to build a website and deployed it. It looks great on your laptop. But what about on a phone? A tablet? A big desktop monitor? You don't own every device — and resizing your browser isn't the same thing.
      </p>
      <p class="text-foreground/60 text-lg leading-relaxed mb-8 reveal reveal-delay-2">
        Viewpo lets you see your site at any screen size, from whatever device you have. No dev tools, no guesswork.
      </p>
      <div class="reveal reveal-delay-3 flex items-center justify-center gap-4">
        <div class="h-px w-12 bg-primary/30"></div>
        <p class="text-primary dark:text-indigo-300 font-heading font-semibold text-lg">
          One app. Every screen size.
        </p>
        <div class="h-px w-12 bg-primary/30"></div>
      </div>
    </div>
  </Section>

  <!-- Benefits -->
  <Section>
    <div class="text-center mb-16">
      <h2 class="font-heading font-bold text-3xl md:text-4xl mb-4 reveal">
        How Viewpo helps
      </h2>
      <p class="text-foreground/60 text-lg max-w-2xl mx-auto reveal reveal-delay-1">
        Preview your website at any screen size, know when your changes go live, and share with anyone.
      </p>
    </div>

    <div class="grid md:grid-cols-2 gap-6 max-w-4xl mx-auto">
      <div class="reveal reveal-delay-1">
        <FeatureCard
          icon="viewport"
          accent="indigo"
          title="See Your Site at Any Screen Size"
          description="Check how your website looks on a phone, tablet, laptop, or 27-inch desktop — all from whatever device you have. Viewpo renders the real site, not a screenshot."
        />
      </div>
      <div class="reveal reveal-delay-2">
        <FeatureCard
          icon="notification"
          accent="orange"
          title="Know When Your Changes Go Live"
          description="Every time you deploy an update, Viewpo shows you the result instantly. No more refreshing dashboards or hunting for preview links."
        />
      </div>
      <div class="reveal reveal-delay-3">
        <FeatureCard
          icon="share"
          accent="emerald"
          title="Share a Preview Link to Get Feedback"
          description="Send a link or QR code to a friend or client. They can see your site at any screen size and tell you what they think — no account required."
        />
      </div>
      <div class="reveal reveal-delay-4">
        <FeatureCard
          icon="oauth"
          accent="sky"
          title="No Complex Setup"
          description="Link your hosting account with a single tap. Viewpo finds your projects automatically. No API keys, no config files, no terminal commands."
        />
      </div>
    </div>
  </Section>

  <!-- Supported Providers -->
  <Section background="muted">
    <div class="text-center mb-12">
      <h2 class="font-heading font-bold text-3xl md:text-4xl mb-4 reveal">
        Works with the tools you already use
      </h2>
      <p class="text-foreground/60 text-lg max-w-2xl mx-auto reveal reveal-delay-1">
        Viewpo connects to the hosting providers used by Cursor, v0, Bolt, Lovable, and Replit.
      </p>
    </div>

    <div class="grid sm:grid-cols-2 gap-4 max-w-2xl mx-auto">
      {providers.map((provider, i) => (
        <div class={`reveal reveal-delay-${Math.min(i + 1, 4)} flex items-center gap-4 px-6 py-4 rounded-xl border border-foreground/10 dark:border-white/10 bg-white dark:bg-slate-800/50`}>
          <div class="w-10 h-10 rounded-lg bg-primary/10 flex items-center justify-center flex-shrink-0">
            <svg class="w-5 h-5 text-primary" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
              <path stroke-linecap="round" stroke-linejoin="round" d="M13.19 8.688a4.5 4.5 0 011.242 7.244l-4.5 4.5a4.5 4.5 0 01-6.364-6.364l1.757-1.757m9.86-9.86a4.5 4.5 0 00-6.364 0l-4.5 4.5a4.5 4.5 0 001.242 7.244" />
            </svg>
          </div>
          <div>
            <p class="font-heading font-semibold text-sm">{provider.name}</p>
            <p class="text-foreground/50 text-xs">{provider.note}</p>
          </div>
        </div>
      ))}
    </div>
  </Section>

  <!-- Before/After -->
  <Section>
    <div class="max-w-4xl mx-auto">
      <h2 class="font-heading font-bold text-3xl md:text-4xl mb-12 text-center reveal">
        Before and after Viewpo
      </h2>
      <div class="grid md:grid-cols-2 gap-8">
        <!-- Before -->
        <div class="reveal reveal-delay-1 rounded-xl border border-foreground/10 dark:border-white/10 bg-white dark:bg-slate-800/50 p-6">
          <div class="flex items-center gap-2 mb-4">
            <div class="w-8 h-8 rounded-lg bg-red-500/10 flex items-center justify-center">
              <svg class="w-4 h-4 text-red-500" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
              </svg>
            </div>
            <h3 class="font-heading font-semibold text-lg">Without Viewpo</h3>
          </div>
          <ul class="space-y-3 text-foreground/60">
            <li class="flex items-start gap-2">
              <span class="text-red-400 mt-1">-</span>
              Resize your browser window and hope it's accurate
            </li>
            <li class="flex items-start gap-2">
              <span class="text-red-400 mt-1">-</span>
              Ask a friend with a different phone to check for you
            </li>
            <li class="flex items-start gap-2">
              <span class="text-red-400 mt-1">-</span>
              Deploy and cross your fingers
            </li>
            <li class="flex items-start gap-2">
              <span class="text-red-400 mt-1">-</span>
              Discover layout issues after someone else finds them
            </li>
          </ul>
        </div>

        <!-- After -->
        <div class="reveal reveal-delay-2 rounded-xl border border-primary/20 bg-primary/5 dark:bg-primary/10 p-6">
          <div class="flex items-center gap-2 mb-4">
            <div class="w-8 h-8 rounded-lg bg-success/10 flex items-center justify-center">
              <svg class="w-4 h-4 text-success" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5">
                <path stroke-linecap="round" stroke-linejoin="round" d="M4.5 12.75l6 6 9-13.5" />
              </svg>
            </div>
            <h3 class="font-heading font-semibold text-lg">With Viewpo</h3>
          </div>
          <ul class="space-y-3 text-foreground/60">
            <li class="flex items-start gap-2">
              <span class="text-success mt-1">+</span>
              Check phone, tablet, and desktop in seconds
            </li>
            <li class="flex items-start gap-2">
              <span class="text-success mt-1">+</span>
              See the real site at the exact resolution — not an approximation
            </li>
            <li class="flex items-start gap-2">
              <span class="text-success mt-1">+</span>
              Share a link and get feedback from anyone
            </li>
            <li class="flex items-start gap-2">
              <span class="text-success mt-1">+</span>
              Deploy with confidence knowing it looks right everywhere
            </li>
          </ul>
        </div>
      </div>
    </div>
  </Section>

  <!-- Pricing note -->
  <Section background="muted" padding="md">
    <div class="max-w-2xl mx-auto text-center reveal">
      <h2 class="font-heading font-bold text-3xl md:text-4xl mb-4">
        Free to start
      </h2>
      <p class="text-foreground/60 text-lg leading-relaxed mb-8">
        The free tier includes up to 3 projects, 1 provider connection, and full access to the viewport viewer at any screen size. No credit card needed. Upgrade to Starter ($9/month) when you need unlimited projects and all providers.
      </p>
      <Button href="/#cta" size="lg" icon="arrow" class="plausible-event-name=Creators+CTA+Free">
        Get Started Free
      </Button>
    </div>
  </Section>

  <!-- FAQ -->
  <Section id="creators-faq">
    <div class="text-center mb-16">
      <h2 class="font-heading font-bold text-3xl md:text-4xl mb-4 reveal">Creator Questions</h2>
      <p class="text-foreground/60 text-lg max-w-2xl mx-auto reveal reveal-delay-1">
        Common questions from people building with AI tools.
      </p>
    </div>

    <div class="max-w-3xl mx-auto space-y-3">
      {creatorFaqItems.map((item, i) => (
        <details class={`accordion-item reveal reveal-delay-${Math.min(i + 1, 4)} group rounded-xl border border-foreground/10 dark:border-white/10 bg-white dark:bg-slate-800/50 overflow-hidden`}>
          <summary class="flex items-center justify-between gap-4 p-5 cursor-pointer select-none list-none font-heading font-semibold text-base hover:bg-foreground/[0.02] dark:hover:bg-white/[0.02] transition-colors">
            {item.question}
            <svg class="w-5 h-5 text-foreground/40 flex-shrink-0 transition-transform duration-200 group-open:rotate-45" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="M12 4.5v15m7.5-7.5h-15" />
            </svg>
          </summary>
          <div class="accordion-content px-5 pb-5 text-foreground/60 leading-relaxed">
            {item.answer}
          </div>
        </details>
      ))}
    </div>
  </Section>

  <!-- FAQPage JSON-LD -->
  <script type="application/ld+json" set:html={JSON.stringify({
    "@context": "https://schema.org",
    "@type": "FAQPage",
    "dateModified": new Date().toISOString().split('T')[0],
    "mainEntity": creatorFaqItems.map((item) => ({
      "@type": "Question",
      "name": item.question,
      "acceptedAnswer": {
        "@type": "Answer",
        "text": item.answer,
      },
    })),
  })} />

  <!-- Final CTA -->
  <Section id="creators-cta" padding="lg" class="relative overflow-hidden">
    <!-- Aurora background -->
    <div class="pointer-events-none absolute inset-0" aria-hidden="true">
      <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[600px] h-[600px] rounded-full bg-primary/15 blur-[150px] dark:bg-primary/10"></div>
      <div class="absolute top-0 right-0 w-[300px] h-[300px] rounded-full bg-secondary/10 blur-[100px] dark:bg-secondary/[0.06]"></div>
      <div class="absolute bottom-0 left-0 w-[250px] h-[250px] rounded-full bg-indigo-300/10 blur-[80px] dark:bg-indigo-400/[0.06]"></div>
    </div>
    <div class="max-w-2xl mx-auto text-center reveal relative z-10">
      <h2 class="font-heading font-bold text-3xl md:text-4xl mb-4">
        Check how your site looks — for free
      </h2>
      <p class="text-foreground/60 text-lg mb-8 leading-relaxed">
        Viewpo is coming soon. Join the beta to try it early, or sign up to be notified when it launches.
      </p>
      <div class="flex flex-col sm:flex-row items-center justify-center gap-4">
        <Button href="/#cta" size="lg" icon="arrow" class="plausible-event-name=Creators+CTA+Join+Beta+Bottom">
          Join the Beta
        </Button>
        <Button href="/" variant="ghost" size="lg">
          Back to Homepage
        </Button>
      </div>
      <p class="mt-6 text-foreground/40 text-sm">
        Work with a team? See <a href="/teams/" class="text-links hover:text-primary transition-colors underline underline-offset-2">Viewpo for Teams</a>.
      </p>
    </div>
  </Section>

  <Footer slot="footer" />
</BaseLayout>
